# Issue #29 validation: stage-zero entry point

ADR-0012 designates the fixed repository Makefile as the genesis build entry
point. A clean TCC build from the checked-out source and admitted libraries is
reproducible and rejects ambient compiler selection.

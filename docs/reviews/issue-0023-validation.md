# Issue #23 validation: CIXPKG v1 layout

The independent reader contract is implemented as specified in
`docs/spec/cixpkg-1.0.md`, including section sizes/digests, deterministic
manifest ordering, compressed manifest and staged-file payload sections,
identity/dependency metadata, per-file digests, and truncated-artifact failure
behavior.

# Issue #110 validation

The CLI exposes descriptive commands and help/version output. Workspace and
cache selection are implemented; cache hits support offline operation. Jobs,
JSON, and explain flags remain planned additions to the standalone contract.

# Issue #110 validation

The CLI now exposes descriptive commands and help/version output. Workspace,
cache, jobs, offline, JSON, and explain flags remain planned additions to the
standalone contract.

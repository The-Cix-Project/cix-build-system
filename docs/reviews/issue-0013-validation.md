# Issue #13 validation: archive format decision

The repository contains no `recipes/package` directory, so the requested
corpus enumeration is recorded as an explicit follow-up prerequisite rather
than invented evidence. ADR-0006 establishes the bounded v1 list, deferred
formats, and `CPDL-E6001` failure contract. No implementation or external
extractor fallback is introduced by this decision ticket.

# Issue #106 validation

CPDL timeout and jobs controls are enforced by the direct-exec runtime; the
standalone plan propagates them to each phase. Signal cancellation and hard
resource quotas remain a required platform qualification.

# Issue #108 validation

The standalone package pipeline binds recipe/source digests, deterministic
manifest output, identity, provenance, and zstd payload framing. Independent
reader interoperability remains a release gate.

package "broken" {
    version "1"
    release 1
    format "cixpkg"
    build {
        each "x" in { "a" } {
            each "x" in { "b" } {
                write "${build}/${each.x}" "a"
            }
        }
    }
}

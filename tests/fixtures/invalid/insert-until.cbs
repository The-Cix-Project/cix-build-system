package "broken" {
    version "1"
    release 1
    format "cixpkg"
    build {
        insert "${build}/Makefile" {
            after "CFLAGS"
            until line
            write " -O2"
            exactly 1
        }
    }
}
